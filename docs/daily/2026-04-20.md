# 2026-04-20 Daily Change Log

## App Structure And Safety

- Split the original single-file app into `index.html`, `assets/app.css`, and `assets/app.js`.
- Removed unsafe modal `eval` routing and hardened inline handler escaping.
- Normalized money values before split math, export rows, totals, QA rows, and transaction display.
- Removed duplicate Plaid sync after token exchange.

## Receipt Parsing And Line Items

- Preserved Sam's instant savings and identifier metadata through receipt splitting.
- Added direct Sam's Club lookup by normalized receipt item number before SerpApi.
- Added Sam's product page title refinement from JSON-LD, social meta tags, or page title.
- Added optional Sam's Advertising Catalog API path for credentialed lookup.
- Added OpenAI-extracted line item fallback for miscellaneous receipts.
- Added server-side receipt math validation in `analyze-receipt` responses.
- Changed OCR input to send OpenAI a Supabase-fetched `data:` URL with inferred MIME type, avoiding failures when OpenAI cannot fetch a signed storage URL directly.
- Added receipt file signature checks and browser-side conversion of HEIC/unsupported image uploads to JPEG before storage.
- Added an analysis-time repair path that replaces already-attached HEIC receipts with browser-converted JPEG copies before OCR.
- Fixed Sam's single-line parsing when product size text, such as `MM 25 SUGAR`, appears before a split price line.
- Added a verified Sam's lookup for item `980066417` as `Member's Mark Premium Cane Sugar, 25 lbs.`.
- Changed Sam's resolution order to try direct/official Sam's item-number lookup before cache, receipt-label, SerpApi, or DuckDuckGo fallbacks.
- Added per-transaction receipt analysis caching so backing out of detected items does not require rerunning OCR/product lookups.
- Updated detected-item category assignment to replace an existing split for the same receipt item instead of duplicating it.
- Updated detected-item name controls so confirmed/verified names show a clear verified button state and badge.
- Added deterministic Ask AI tax-guidance rules for common Schedule C categories, including worker coffee/refreshments as Meals with review instead of Office Supplies.
- Hardened miscellaneous receipt analysis for receipts without product numbers, including Patreon-style invoice totals and synthetic receipt line IDs.
- Added Ask AI guidance for business-use digital memberships/content subscriptions and reinforced that every prompt should evaluate the full category set.
- Audited existing non-Sam's/non-Walmart receipt attachments: 27 checked, all valid supported PDF/JPEG/PNG files.
- Updated single-line misc service receipts, such as Patreon, to display the merchant as the detected line name while preserving the invoice description in raw context.
- Hardened receipt total parsing so tax summary rows and subtotal/tax/total tables do not misread tax as subtotal or total.
- Added subtotal completion from parsed receipt line items so misc receipts without an explicit subtotal row do not display `0.00`.

## Supabase And Deployment

- Added function tests to the Supabase deploy workflow.
- Added optional migration push when `SUPABASE_DB_URL` is configured.
- Reduced raw/sensitive logging in Plaid and receipt functions.

## Validation

- Added `scripts/validate.sh` for local validation.
- Current validation checks:
  - `node --check assets/app.js`
  - `deno check supabase/functions/**/*.ts`
  - `deno test supabase/functions/**/*.test.ts`
  - `git diff --check`
